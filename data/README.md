# data/ — build metadata

**Generated by `./scripts/build.sh`** from `geotiffs/*.tif`.

| File | Purpose |
|------|---------|
| `catalog.json` | Granule list, dates, bounds — input for `docs/js/catalog.js` |

This file is gitignored. A copy is also written to `docs/data/catalog.json` for the live site.
